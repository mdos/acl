module.exports = {
    _roles : [],
    
    init_acl : function () {
        // load up the ACL, make it read only
    },

    num_roles : function() {
        return this._roles.length();
    }

}

